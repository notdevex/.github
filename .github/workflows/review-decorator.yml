name: Review Decorator

on:
  pull_request_review:
    types: [submitted]


jobs:
  review-decorator:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2

      - name: Run Action
        uses: nodevex/.github/actions/add-gif@git-comment
