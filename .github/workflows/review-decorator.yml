name: Review Decorator

on:
  pull_request_review:
    types: [submitted]


jobs:
  review-decorator:
    runs-on: ubuntu-latest
    steps:
      - name: Run Action
        uses: notdevex/.github/actions/add-gif@git-comment
